---
description: Auto git deploy after agent code changes
alwaysApply: true
---

# Deploy after changes

Do not `git add`, `git commit`, or `git push`. The stop hook `.cursor/hooks/commit-and-push.sh` does that.

Do not run `clasp push` locally. GitHub Actions does it on push to `main` (`.github/workflows/clasp-push.yml`).
